import SwiftUI
// TODO: This view will combine all industry section views, depending on which industry is selected, this view will determine which industries section will show.
struct IndustryFieldsView {
    var body: some View {
        Text("IndustryFieldsView")
    }
}
